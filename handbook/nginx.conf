server {
    listen 8000;
    root /var/www;
    index index.html;
    location / {
        try_files $uri $uri/ =404;
    }
    location = /healthz {
        add_header Content-Type text/plain;
        return 200 'ok';
    }
}
